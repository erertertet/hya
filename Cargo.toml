# hya — Rust multi-agent coding agent. Workspace root.
# See CONTEXT.md for the full architecture and agent-spec contract.
[workspace]
resolver = "3"
members = ["crates/*"]
exclude = ["vendor/sevenz-rust2-0.20.2"]

[workspace.package]
version = "0.37.5"
edition = "2024"
rust-version = "1.91"
license = "MIT OR Apache-2.0"

[workspace.dependencies]
# --- internal crates (path deps) ---
hya-api = { path = "crates/hya-api" }
hya-bundle = { path = "crates/hya-bundle" }
hya-proto = { path = "crates/hya-proto" }
hya-provider = { path = "crates/hya-provider" }
hya-tool = { path = "crates/hya-tool" }
hya-store = { path = "crates/hya-store" }
hya-core = { path = "crates/hya-core" }
hya-workflow = { path = "crates/hya-workflow" }
hya-server = { path = "crates/hya-server" }
hya-mcp = { path = "crates/hya-mcp" }
hya-plugin = { path = "crates/hya-plugin" }
hya-plugin-claude = { path = "crates/hya-plugin-claude" }
hya-app = { path = "crates/hya-app" }
# --- external (pinned intent; each crate opts in per phase via `{ workspace = true }`) ---
tokio = { version = "1", features = ["full", "test-util"] }
tokio-util = { version = "0.7", features = ["rt"] }
tokio-stream = { version = "0.1", features = ["sync"] }
tokio-tungstenite = "0.24"
futures = "0.3"
async-trait = "0.1"
base64 = "0.22"
sha2 = "0.10"
ed25519-dalek = "2"
axum = { version = "0.7", features = ["macros", "ws"] }
tower = { version = "0.5", features = ["util"] }
tower-http = { version = "0.6", features = ["trace", "cors"] }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls", "stream"] }
eventsource-stream = "0.2"
httpdate = "1"
http-body-util = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
serde_norway = "0.9" # maintained serde_yaml fork — YAML config parsing
regex = "1"
rand_core = { version = "0.9", features = ["os_rng"] }
similar = "2"
xxhash-rust = { version = "0.8", features = ["xxh32"] }
mime_guess = "2.0.5"
libc = "0.2"
pty-process = "0.5.3"
sqlx = { version = "0.8", features = ["runtime-tokio", "sqlite", "json", "uuid", "time", "macros", "migrate"] }
thiserror = "1"
anyhow = "1"
uuid = { version = "1", features = ["v4", "v5", "v7", "serde"] }
time = { version = "0.3", features = ["serde", "macros", "parsing", "formatting"] }
tracing = "0.1"
tokenizers = { version = "0.21", default-features = false, features = ["fancy-regex"] }
hf-hub = "0.4"
clap = { version = "4", features = ["derive", "env"] }
toml = "0.8"
secrecy = "0.8"
sevenz-rust2 = { path = "vendor/sevenz-rust2-0.20.2", version = "=0.20.2", default-features = false }
# --- v1 dual-protocol contract (hya-api runtime + xtask codegen) ---
prost = "0.13"
prost-types = "0.13"
pbjson = "0.7"
tonic = "0.13"
pbjson-types = "0.7"
tonic-build = "0.13"
pbjson-build = "0.7"
protoc-bin-vendored = "3"

# Library crates deny panic-paths (parse-don't-validate; typed errors). Bins/tests
# may relax locally with #![allow(...)]. (Per design.md N3 / implement.md gate.)
[workspace.lints.rust]
# Documentation coverage gate. Every public item in the workspace carries a doc
# comment; `deny` keeps it that way.
missing_docs = "deny"

[workspace.lints.clippy]
unwrap_used = "deny"
expect_used = "deny"

[profile.dev]
# faster incremental builds during the long phased build-out
opt-level = 0

[profile.release]
# Cold start is dominated by pre-main work (page-in, code-signature
# verification, dyld fixups) that scales with binary size; stripping local
# symbols cuts the release binary ~22% with no runtime behavior change.
strip = "symbols"
